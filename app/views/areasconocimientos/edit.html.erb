<div class="container">
	<div class="form-horizontal">

	<!-- Form Name -->
		<legend ><h2>Editar detalles del Area de conocimiento</h2></legend>
		<%= form_for @areasconocimiento, :url =>{:action => "update", :id =>@areasconocimiento} do |f| %>
		<% if @areasconocimiento.errors.any? %>
			    <% @areasconocimiento.errors.full_messages.each do |message| %>
			    	<%if message != "User has already been taken" %>
			    	<div class="alert alert-danger" role="alert">
	  					<li><%= message %></li>
					</div>
			      	<%end%>	
			    <% end %>
		  	 <% end %>
		 <div class="form-group">
						<div class="col-md-5">
						  	<strong>ID: </strong> <%= @areasconocimiento.id %><br />
						</div>  	
		 </div>   	 

			
		 <div class="form-group">
						<div class="col-md-5">
						  	<label for="name" >Nombre:</label>  
						  	<%= f.text_field  :name, :class => "form-control " %>
						</div>  	
		 </div> 
		 <div class="form-group" >
					<div class="col-md-2">
					  <%= f.submit "Guardar cambios" , class: "btn btn-primary" %>
					</div>  	
		 </div>
		
		<% end %>

		<%= link_to 'Atrás', {:action => 'index' } %>
	</div>
</div>	