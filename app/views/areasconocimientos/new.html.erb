
<div class="container">
	<div class="form-horizontal">

		<!-- Form Name -->
		<legend ><h2>Nueva area de conocimiento</h2></legend>
		<%= form_for @areasconocimiento, :url =>{:action => "create", :id =>@areasconocimiento} do |f| %>
		     <% if @areasconocimiento.errors.any? %>
			    <% @areasconocimiento.errors.full_messages.each do |message| %>
			    	<%if message != "User has already been taken" %>
			      		<div class="alert alert-danger" role="alert">
		  					<%= message %>
						</div>
			      	<%end%>	
			    <% end %>
		  	 <% end %>
		  	 <div class="form-group">
					<div class="col-md-5">
					  	<label for="name" >Nombre:</label>  
					  	<%= f.text_field  :name, :class => "form-control " %>
					</div>  	
			 </div>
			 <div class="form-group" >
					<div class="col-md-2">
					  <%= f.submit "Crear" , class: "btn btn-primary" %>
					</div>  	
			 </div>
				

		 <% end %>
				<%= link_to 'Atrás', {:action => 'index'} %>
	</div>
</div>		
